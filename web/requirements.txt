# Web UI (ValorLink Regimental Headquarters). Install alongside the bot's
# requirements.txt; both share db/ and config.py.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
itsdangerous>=2.1.0   # signed session cookies (Starlette SessionMiddleware)
httpx>=0.27.0         # Discord OAuth2 token/userinfo calls
python-multipart>=0.0.9  # form parsing for the officer action endpoints
